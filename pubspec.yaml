name: MicheleD
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^3.0.4
  flutter_launcher_icons: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2

flutter_icons:
  android: true # Set to true to update the Android icon
  ios: true # Set to true to update the iOS icon
  image_path: "assets/icon.png"

flutter:
  uses-material-design: true
  assets:
    - assets/